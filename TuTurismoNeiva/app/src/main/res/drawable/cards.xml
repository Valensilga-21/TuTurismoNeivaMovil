<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Color de fondo de la CardView -->
    <stroke
        android:width="5dp"
        android:color="@color/naranja" /> <!-- Color del borde (rojo en este caso) -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" />
</shape>
